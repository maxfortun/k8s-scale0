apiVersion: v1
kind: Service
metadata:
  name: {{ include "scale0.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "scale0.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scale0.selectorLabels" . | nindent 4 }}
