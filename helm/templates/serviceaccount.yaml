{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "scale0.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "scale0.labels" . | nindent 4 }}
{{- end }}
