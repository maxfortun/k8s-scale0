apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: stateful-app
  namespace: stateful-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: stateful-app
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
